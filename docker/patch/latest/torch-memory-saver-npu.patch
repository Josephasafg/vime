diff --git a/contrib/torch_memory_saver/csrc/entrypoint.cpp b/contrib/torch_memory_saver/csrc/entrypoint.cpp
index 1111111..2222222 100644
--- a/contrib/torch_memory_saver/csrc/entrypoint.cpp
+++ b/contrib/torch_memory_saver/csrc/entrypoint.cpp
@@ -38,6 +38,18 @@ static thread_local ThreadLocalConfig thread_local_config;
 // ------------------------------------------------
 
 #ifdef TMS_HOOK_MODE_PRELOAD
+aclError aclrtMalloc(void **ptr, size_t size,
+                     aclrtMemMallocPolicy policy) {
+  if (thread_local_config.is_interesting_region()) {
+    return TorchMemorySaver::instance().malloc(
+        ptr, CANNUtils::cann_ctx_get_device(), size,
+        thread_local_config.current_tag_,
+        thread_local_config.enable_cpu_backup());
+  } else {
+    return APIForwarder::call_real_aclrt_malloc(ptr, size, policy);
+  }
+}
+
 aclError aclrtMallocAlign32(void **ptr, size_t size,
                             aclrtMemMallocPolicy policy) {
   if (thread_local_config.is_interesting_region()) {
diff --git a/contrib/torch_memory_saver/csrc/api_forwarder.h b/contrib/torch_memory_saver/csrc/api_forwarder.h
index 3333333..4444444 100644
--- a/contrib/torch_memory_saver/csrc/api_forwarder.h
+++ b/contrib/torch_memory_saver/csrc/api_forwarder.h
@@ -2,6 +2,8 @@
 #include <acl/acl.h>
 #include <dlfcn.h>
 namespace APIForwarder {
+aclError call_real_aclrt_malloc(void **ptr, size_t size,
+                                aclrtMemMallocPolicy policy);
 aclError call_real_aclrt_malloc_align32(void **ptr, size_t size,
                                         aclrtMemMallocPolicy policy);
 aclError call_real_aclrt_free(void *ptr);
diff --git a/contrib/torch_memory_saver/csrc/api_forwarder.cpp b/contrib/torch_memory_saver/csrc/api_forwarder.cpp
index 5555555..6666666 100644
--- a/contrib/torch_memory_saver/csrc/api_forwarder.cpp
+++ b/contrib/torch_memory_saver/csrc/api_forwarder.cpp
@@ -6,6 +6,7 @@
 namespace APIForwarder {
 
 using AclrtMallocFuncAlign32 = aclError (*)(void **, size_t,
                                             aclrtMemMallocPolicy);
+using AclrtMallocFunc = aclError (*)(void **, size_t, aclrtMemMallocPolicy);
 using AclrtFreeFunc = aclError (*)(void *);
 
 static void *check_dlsym(void *value) {
@@ -18,8 +19,24 @@ static void *check_dlsym(void *value) {
   return value;
 }
 
+static AclrtMallocFunc real_aclrt_malloc_ = NULL;
 static AclrtMallocFuncAlign32 real_aclrt_malloc_align32_ = NULL;
 static AclrtFreeFunc real_aclrt_free_ = NULL;
 
+aclError call_real_aclrt_malloc(void **ptr, size_t size,
+                                aclrtMemMallocPolicy policy) {
+  static std::once_flag malloc_flag;
+  std::call_once(malloc_flag, []() {
+    real_aclrt_malloc_ =
+        (AclrtMallocFunc)check_dlsym(dlsym(RTLD_NEXT, "aclrtMalloc"));
+  });
+
+  aclError ret = real_aclrt_malloc_(ptr, size, policy);
+
+#ifdef TMS_DEBUG_LOG
+  std::cout << "[torch_memory_saver.cpp] aclrtMalloc [MODE NORMAL]"
+            << " ptr=" << ptr << " *ptr=" << *ptr << " size=" << size
+            << " ret=" << ret << std::endl;
+#endif
+
+  return ret;
+}
+
 aclError call_real_aclrt_malloc_align32(void **ptr, size_t size,
